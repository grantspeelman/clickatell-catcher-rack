source 'https://rubygems.org'

# Specify your gem's dependencies in clickatell-catcher-rack.gemspec
gemspec

gem 'rack', ENV['GEMFILE_RACK_VERSION'] if ENV['GEMFILE_RACK_VERSION']

if ENV['GEMFILE_MULTI_JSON_VERSION']
  gem 'multi_json', ENV['GEMFILE_MULTI_JSON_VERSION']
end
