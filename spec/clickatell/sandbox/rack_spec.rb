require 'spec_helper'

describe Clickatell::Sandbox::Rack do
  it 'has a version number' do
    expect(Clickatell::Sandbox::Rack::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
